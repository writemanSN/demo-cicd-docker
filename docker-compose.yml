services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      DB_HOST: host.docker.internal
      DB_NAME: tester
      DB_PASSWORD: ""

  frontend:
    build: ./frontend
    ports:
      - "3000:80"